{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Run App",
            "request": "launch",
            "mainClass": "com.sts.App",
            "classPaths": [
                "${workspaceFolder}/bin",
                "${workspaceFolder}/lib/mssql-jdbc.jar"
            ]
        }
    ]
}
