{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Java", // Biarkan label tetap sama
            "type": "shell",
            "command": "mvn clean install", // Ganti 'javac' dengan 'mvn clean install'
            "group": "build",
            "problemMatcher": []
        }
    ]
}